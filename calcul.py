def substract (number_one, number_two):
    result = number_one - number_two
    return result
calcul = substract(7,2)
print(calcul)

def multiply (number_one, number_two, number_three):
    return number_one * number_two * number_three
calcul = "The results is " + str(multiply(2,4,5)) + "!"
print(calcul)
